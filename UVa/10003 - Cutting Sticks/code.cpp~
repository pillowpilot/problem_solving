#include <cstdio>
#include <vector>

using namespace std;

typedef vector<int> vi;
typedef vector<vi> vvi;

vvi memo;

void init()

int main(){
  int l, c;
  scanf("%d", &l);
  while( l != 0 ){
    
    scanf("%d", &l);
  }
}
