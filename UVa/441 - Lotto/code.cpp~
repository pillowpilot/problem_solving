#include <cstdio>
#include <vector>
#include <algorithm>

using namespace std;

typedef vector<int> vi;

vi s;

int main(){
  int k;
  scanf("%d", &k);
  while( k != 0 ){
    scanf("%d", &k);
  }
  return 0;
}
