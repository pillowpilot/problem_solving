#include <cstdio>
#include <vector>

using namespace std;

struct Edge{
  int cap, flow;
  Edge(){
    cap = flow = 0;
  }
  Edge(int cap, int flow):cap(cap), flow(flow) {}
};



int main(){
  
  return 0;
}
