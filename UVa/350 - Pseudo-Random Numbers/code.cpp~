#include <cstdio>
#include <vector>

using namespace std;

int main(){
  int multi, incre, mod, seed;

  while( scanf("%d %d %d %d", &multi, &incre, &mod, &seed) == 4 && multi != 0 && incre != 0 && mod != 0 && seed != 0 ){
    
  }
  return 0;
}
