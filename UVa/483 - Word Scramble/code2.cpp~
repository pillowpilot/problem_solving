#include <iostream>
#include <string>

using namespace std;

int main(){
  istringstream iss;
  string line, word;
  
  while(getline(cin, line){
      iss.str(line);
      while(iss >> word){
	reverseWord(word);
      }
    }
  return 0;
}
