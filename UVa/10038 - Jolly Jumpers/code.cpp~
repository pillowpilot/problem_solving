#include <stdio.h>
#include <bitset>

using namespace std;

int main(){
  long n, a, b;
  bitset<3000> diferences;

  while(scanf("%ld ", &n) == 1){
    scanf("%ld", &a);
    for(int i = 0; i < n< i++){
      scanf("%ld", &b);
  return 0;
}
