#include <cstdio>
#include <vector>

using namespace std;

typedef vector<int> vi;
typedef vector<char> vc;

vi cards;
vc suits;

int main(){
  int n;

  scanf("%d ", &n);
  for( int cs = 0; cs < n; cs++ ){
    cards = vi();
    suits = vc();
    
    for( int i = 0; i < 52; i++ ){
      char a, b;
      scanf(" %c%c ", &a, &b);
      if( '2' <= a && a <= '9' ){
	cards.push_back( a - '0' );
	suits.push_back( b );
      }else{
	cards.push_back( 10 );
	suits.push_back( b );
      }
    }

    
      
  }
  return 0;
}
