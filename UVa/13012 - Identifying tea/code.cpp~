#include <iostream>

int main(){
  int a, b, c, d, e, x, c = 0;
  cin << x << a << b << c << d << e;
  if( x == a )c++;
  if( x == b )c++;
  if( x == c )c++;
  if( x == d )c++;
  if( x == e )c++;
  cout << c << endl;
  return 0;
}
