#include <iostream>
#include <string>
#include <map>

using namespace std;

typedef struct{
  char next;
} vertex;

void initGraph(map<char, vertex> &vs){
  vs.clear();
}

int main(){
  string a, b;
  
  cin >> a;
  while( a != "#" ){
    cin >> b;

    cin >> a;
  }
}
