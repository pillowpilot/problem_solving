#include <cstdio>
#include <string>

using namespace std;

int main(){
  int c;
  char s[55];

  scanf("%d ", &c);
  while( scanf("%[^\n]\n", s) == 1 && strlen(s) > 0 )
    printf(">>%s<<\n", s);
  return 0;
}
