#include <iostream>

using namespace std;

int main(){
  std::ios_base::sync_with_stdio(false);
  
  int sum, dif, n;
  
  cin >> n;
  while( n-- > 0 ){
    cin >> sum >> dif;
    
  }
  
  
  return 0;
}
