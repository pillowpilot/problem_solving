#include <cstdio>
#include <vector>

using namespace std;

int main(){
  int n, l;

  while( scanf("%d", &n) == 1 && n != 0 ){
    int l;
    scanf("%d", &l);

    while( l-- > 0 ){
      
    }
  }
  return 0;
}
