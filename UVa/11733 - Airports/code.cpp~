#include <cstdio>
#include <vector>
#include <map>

using namespace std;

int main(){
  int tests, n, m, a;
  vector<bool> marked;
  map< int, pair<int, int> > roads;
  
  scanf("%d ", &test);
  for(int testNumber = 1; testNumber <= test; testNumber++){
    scanf("%d %d %d ", &n, &m, &a);
    

    
  }
  return 0;
}
