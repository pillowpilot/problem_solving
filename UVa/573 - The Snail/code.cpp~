#include <cstdio>

using namespace std;

int main(){
  int height = 1, dDay, dNight, fFactor;
  
  while(height != 0){
    scanf("%d %d %d %d", &height, &dDay, &dNight, &fFactor);
    if(height != 0){
      
    }
  }
  
  return 0;
}
