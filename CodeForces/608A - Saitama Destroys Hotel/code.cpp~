#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
  ios_base::sync_with_stdio(false);
  int n, s;
  cin >> n >> s;
  vector<int> pp(s);
  while(n-->0){
    int t, f;
    cin >> t >> f;
    
  }
  
  return 0;
}
