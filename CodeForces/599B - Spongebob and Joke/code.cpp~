#include <iostream>
#include <vector>

using namespace std;

typedef vector<int> vi;

vi ff;
vi bb;

int main(){
  int n, m;

  ff = vi();
  bb = vi();

  while( n-- > 0 ){
    int a;
    cin >> a;
    ff.push_back( a );
  }
  while( m-- > 0 ){
    int a;
    cin >> a;
    bb.push_back( a );
  }
  n = ff.size();
  m = bb.size();

  
  
  return 0;
}
