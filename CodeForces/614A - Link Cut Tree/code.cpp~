#include <iostream>

using namespace std;

typedef unsigned long long ull;

int main(){
  ios_base::sync_with_stdio(false);
  ull l, r, k;

  cin >> l >> r >> k;
  while( k < l ) k *= k;
  while( k <= r ){
    cout << k << " ";
    k *= k;
  }
  cout << endl;
  return 0;
}
