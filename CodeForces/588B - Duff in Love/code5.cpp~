#include <iostream>

usign namespace std;

typedef unsigned long long ull;


int main(){
  ull n, a = 1;

  cin >> n;

  ull p = 2;
  while( n > 1 ){
    if( n % p == 0 ){
      a *= p;
    }
    while( n % p == 0 ){
      n /= p;
    }
  }
  return 0;
}
