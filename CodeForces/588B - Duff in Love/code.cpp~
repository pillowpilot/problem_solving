#include <iostream>
#include <vector>

using namespace std;

typedef vector<bool> vb;

vb t;

int main(){
  int n;

  cin >> n;

  t = vb( 1e6 );
  while( n != 1 ){
    int d = 2;
    while( n % d != 0 ) d--;
    n /= d;
  }
  
  
  return 0;
}
