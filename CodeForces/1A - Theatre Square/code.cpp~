#include <cstdio>

usign namespace std;

int solve(int n, int m, int a){
  long long r = (n*m) % (a*a);
  if( r == 0 ) r = (n*m) / (a*a);
  else r = (n*m)/(a*a) + 1;
}

int main(){
  int m, n, a, r;
  scanf("%d %d %d", &n, &m, &a);
  printf("%d\n", solve(n, m, a));
  return 0;
}
