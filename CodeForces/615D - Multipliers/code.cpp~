#include <iostream>
#include <map>

#define PRIME (1e9+7)

using namespace std;

typedef unsigned long long ull;
typedef map<ull, int> mui;

mui m;

int main(){
  ull n, p, pp;
  cin >> n;
  while( n-- > 0 ){
    cin >> p;
    p = p % PRIME;
    pp = pp * p;
    pp = pp % PRIME;
  }
  return 0;
}
