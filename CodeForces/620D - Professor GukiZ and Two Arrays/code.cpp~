#include <iostream>
#include <vector>

using namespace std;

typedef long long ll;
typedef vector<ll> vll;

vll a, b;

int main(){
  int n, m;

  cin >> n;
  a = vll(n);
  for(int i = 0; i < n; i++){
    ll x;
    cin >> x;
    a[i] = x;
  }

  cin >> m;
  b = vll(m);
  for(int i = 0; i < m; i++){
    ll x;
    cin >> x;
    b[i] = x;
  }

  ll m1, m2, m3;
  m1 = m2 = m3 = -1e9;

  for(int i = 0; i < n; i++){
    for(int j = 0; j < m; j++){
      
    }
  }
  
  return 0;
}
