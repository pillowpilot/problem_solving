#include <iostream>
#include <string>
#include <algorithm>

using namespaces std;

int main(){
  std::ios_base::sync_with_stdio(false);
  int n, t;
  String a, b;

  cin >> n >> t >> a >> b;

  sort(a.begin(), a.end());
  sort(b.begin(), b.end());

  
  return 0;
}
