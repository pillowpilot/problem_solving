#include <iostream>
#include <cmath>

using namespace std;

int main(){
  int n;

  cin >> n;
  while( n-- > 0 ){
    int x;
    cin >> x;
    if( sqrt(x)*sqrt(x) == x ) cout << "YES\n";
    else cout << "NO\n";
  }
  cout << flush;
  return 0;
}
