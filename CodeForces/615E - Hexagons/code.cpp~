#include <iostream>

using namespace std;

typedef long long ll;

ll ring(ll n){
  ll x = 0;
  for(int i = 0; i < 50; i++){
    if( x*x*3 + x*3 + 1 < n )
      x = x + (n-x)/2;
    else
      x = x - (n-x)/2;
  }
  return x;
}

int main(){
  ll n;
  cin >> n;
  
  return 0;
}
