#include <cstdio>
#include <vector>

using namespace std;

struct Node{
  size_t left, right;
  int value;
  Node(){ left = right = value = 0;}
  Node(size_t left, size_t right, int value): left(left), right(right), value(value) {}
};

int main(){
  return 0;
}
