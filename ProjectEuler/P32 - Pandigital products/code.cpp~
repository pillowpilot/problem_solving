#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){

  vector<int> perm{1, 2, 3, 4, 5, 6, 7, 8, 9};

  do{
    
  }while( next_permutation(perm) );
  
  return 0;
}
