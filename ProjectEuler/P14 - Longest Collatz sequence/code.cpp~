#include <iostream>
#include <vector>

using namespace std;

vector<int> dp;

int len(int n){
  if( dp[n] != 0 )
    return dp[n];
  else{
    int l = len( (n%2==0)?n/2:3*n+1 ) + 1;
    dp[n] = l;
    return l;
  }
}

int main(){
  dp = vector<int>(1e6+14, 0);
  
}
