#include <iostream>

using namespace std;

bool isPalindrome(int n){
  int a = n, b = 0;
  while( n != 0 ){
    int d = n % 10;
    b = b*10 + d;
    n = n / 10;
  }
  return a == b;
}

int main(){
  
  return 0;
}
