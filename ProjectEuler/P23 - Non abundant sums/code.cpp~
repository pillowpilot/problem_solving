#include <iostream>
#include <vector>

using namespace std;

int d(int n){
  int i = 2, sum = 1;
  while( i*i <= n ){
    if( n % i == 0 )
      sum += i + n / i;
    i++;
  }
  return sum;
}

int main(){
  return 0;
}
