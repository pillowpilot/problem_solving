#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;

struct Data{
  int sa, msd, lsd;
  Data(){ sa = msd = lsd = 0; }
  Data(int sa, int msd, int lsd): sa(sa), msd(msd), lsd(lsd) {}
};

int main(){
  return 0;
}
